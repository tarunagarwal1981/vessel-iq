{
  "name": "vessel-iq",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start"
  },
  "dependencies": {
    "next": "^12.0.0",       // Next.js framework version
    "react": "^17.0.0",      // React library version
    "react-dom": "^17.0.0"   // React DOM library version
  }
}
